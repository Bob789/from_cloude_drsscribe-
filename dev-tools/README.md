# Dev-Tools (internal)

Internal Playwright-based service that captures full-page screenshots and
produces self-contained HTML "flatten" snapshots of any URL — particularly
useful for Flutter Web pages where the rendered DOM is hidden behind CanvasKit.

**Not exposed in production. Dev environment only.**

See `docs/DEV_TOOLS_GUIDE.md` for usage.
